apiVersion: 1

policies:
  - orgId: 1
    receiver: slack
    group_by: [alertname, env]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 12h
    routes:
      - receiver: slack
        object_matchers:
          - ['severity', '=', 'critical']
        repeat_interval: 1h
